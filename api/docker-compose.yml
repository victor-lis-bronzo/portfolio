services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp_api
    restart: always
    environment:
      - PORT=3001
      - NODE_ENV=production
    expose:
      - "3001"
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
