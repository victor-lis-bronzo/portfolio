import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	// No custom headers, redirects, or regions in use, so no vercel.json is needed for deploy.
};

export default nextConfig;
